# Core deps
numpy
opencv-python
pdf2image
paddleocr
paddlepaddle==2.6.2
paddleocr==2.9.1
PyMuPDF==1.26.3
opencv-python==4.12.0.88
numpy==1.26.4
# Optional: faster image ops / common utilities used by OCR stacks
# (not required, but harmless if compatible)
Pillow

