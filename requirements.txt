# --- OCR / PDF core ---
# ocr_pipeline.py uses paddleocr's 3.x pipeline API (.predict(), PP-OCRv5
# model names, use_doc_orientation_classify, ...) which only exists from
# paddleocr>=3.0.0 - do not downgrade paddleocr/paddlepaddle below 3.x
# without re-checking ocr_pipeline.py's API usage.
#
# numpy/opencv/PyMuPDF are intentionally NOT hard-pinned here: the verified
# working versions (numpy 2.3.5, opencv-python 5.0.0.93) require Python
# >=3.11, which venv310/CI (Python 3.10) can't install. Left unpinned so
# pip resolves whatever numpy/opencv build paddleocr 3.7.0 / paddlepaddle
# 3.0.0 actually need for the interpreter in use.
paddlepaddle==3.0.0
paddleocr==3.7.0
opencv-python
PyMuPDF
pdf2image

# --- Utilities ---
Pillow
python-dotenv
pymongo
dnspython
requests

# --- Scraper ---
playwright
playwright-stealth

# --- Google Drive ---
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# --- Vector DB ---
pinecone

# --- Local embedding search (Streamlit "Search Vector DB" feature in main.py) ---
torch==2.6.0
sentence-transformers
